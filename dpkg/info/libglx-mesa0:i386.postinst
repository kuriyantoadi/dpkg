#!/bin/sh

set -e

case "$1" in
  configure)
  # remove alternatives from libgl1-mesa-glx
  update-alternatives --remove i386-linux-gnu_gl_conf \
	/usr/lib/i386-linux-gnu/mesa/ld.so.conf

  # explicit ldconfig due to alternatives
  ldconfig

esac



exit 0
