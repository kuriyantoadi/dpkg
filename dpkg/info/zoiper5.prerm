#!/bin/sh

APPLICATION_NAME="zoiper5"
INSTALL_DIR=/usr/bin

if [ -e "${INSTALL_DIR}/${APPLICATION_NAME}" ]; then
  rm -r "${INSTALL_DIR}/${APPLICATION_NAME}"
fi

if [ -e "${INSTALL_DIR}/${APPLICATION_NAME}.tar.xz" ]; then
  rm -r "${INSTALL_DIR}/${APPLICATION_NAME}.tar.xz"
fi
